# Docs

This repo is a CLI for piping command output through an LLM.

Pick the page that matches what you are trying to do:

- Run it (install + first prompt): [`docs/setup.md`](setup.md)
- Integrate it (pipelines + stdout/stderr contract): [`docs/integration.md`](integration.md)
- Change it (build/test/dev notes): [`docs/contributing.md`](contributing.md)
